openapi: 3.1.0
info:
  title: Task API
  version: 1.0.0
servers:
  - url: http://localhost:8080
    description: local dev servers
  - url: https://task-api-kwr6.onrender.com/tasks

    description: production server on render
tags:
  - name: tasks
    description: task related endpoints
paths:
  /tasks:
    $ref: ./paths/tasks.yaml
